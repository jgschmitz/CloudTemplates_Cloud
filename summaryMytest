#lets make this simpler and clean it up

# Get stack details and extract key information using AWS CLI and jq
aws cloudformation describe-stacks --stack-name myteststack \
    --query "Stacks[0].{StackId:StackId, StackName:StackName, Status:StackStatus, CreatedOn:CreationTime, Outputs:Outputs}" \
    --output json
